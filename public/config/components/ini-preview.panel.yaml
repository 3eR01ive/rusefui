# Временная вкладка: все панели настроек из INI текущего проекта (user cache).
id: ini-preview.panel
description: Превью INI → YAML

children:
  - type: text
    props:
      text: "Панели генерируются из INI проекта в ~/.rusEFI/projects/{project}/ui_panels/{ini_hash}/."
      variant: hint

  - type: ini-panels-browser
    id: browser
    navSelectable: false
