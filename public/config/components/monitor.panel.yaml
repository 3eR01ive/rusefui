# Монитор: live values + график output channels.
id: monitor.panel
description: Output channels — значения и кривые

children:
  - type: section
    props:
      title: Log
    children:
      - type: output-chart
        id: scope
        props:
          height: 300
          windowSeconds: 30
          fields:
            - RPMValue
            - coolant

  - type: section
    props:
      title: Live values
    children:
      - type: row
        children:
          - type: output-value
            id: rpm
            props:
              label: RPM
              unit: rpm
              decimals: 0
            bind:
              source: outputChannels
              field: RPMValue

          - type: output-value
            id: clt
            props:
              label: CLT
              unit: "°C"
              decimals: 1
            bind:
              source: outputChannels
              field: coolant

      - type: text
        props:
          text: "Опрос outputChannels (команда O) в Rust ~10 Hz. INI подбирается по signature ECU."
          variant: hint
